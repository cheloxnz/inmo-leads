version: "3.9"
services:
  inmobot-backend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: inmobot-backend
    restart: unless-stopped
    ports:
      - "8002:8002"
    env_file:
      - .env
